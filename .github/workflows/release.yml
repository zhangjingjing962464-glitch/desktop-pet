name: Release

# 触发：push 形如 v0.1.0 / v1.2.3 的 tag。
# Mac dmg 仍由开发者本地 build（含模型，文件大、版权敏感），通过 gh release 上传；
# 本 workflow 负责 Windows / Linux。
# CI runner 通过 secrets.MODELS_TOKEN 拉私有 repo desktop-pet-models 的 .glb，
# 再打包进 exe / AppImage —— 用户开箱即用，仓库本身不暴露模型。

on:
  push:
    tags: ['v*']
  # 允许从 GitHub Actions 页面手动触发，方便测试 CI 链路不走 tag
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Fetch models from private repo
        shell: bash
        env:
          MODELS_TOKEN: ${{ secrets.MODELS_TOKEN }}
        run: |
          git clone --depth 1 \
            "https://x-access-token:${MODELS_TOKEN}@github.com/zhangjingjing962464-glitch/desktop-pet-models.git" \
            _models
          mkdir -p assets/models
          cp _models/*.glb assets/models/
          rm -rf _models
          ls assets/models | wc -l

      - run: npm run scripts:manifest

      - name: Build Windows NSIS installer
        run: npm run dist:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: List build artifacts
        shell: pwsh
        run: Get-ChildItem release | Format-Table -AutoSize

      # workflow_dispatch 时（没 tag）只上传到 actions artifact，方便手动测试下载验证
      - name: Upload .exe as Actions artifact
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: desktop-pet-windows
          path: |
            release/*.exe
            release/*.exe.blockmap

      # tag push 时上传到对应 GitHub Release
      - name: Upload .exe to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/*.exe
            release/*.exe.blockmap
          fail_on_unmatched_files: false
          token: ${{ secrets.GITHUB_TOKEN }}

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Fetch models from private repo
        env:
          MODELS_TOKEN: ${{ secrets.MODELS_TOKEN }}
        run: |
          git clone --depth 1 \
            "https://x-access-token:${MODELS_TOKEN}@github.com/zhangjingjing962464-glitch/desktop-pet-models.git" \
            _models
          mkdir -p assets/models
          cp _models/*.glb assets/models/
          rm -rf _models
          ls assets/models | wc -l

      - run: npm run scripts:manifest

      - name: Build Linux AppImage
        run: npm run dist:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: List build artifacts
        run: ls -la release/

      - name: Upload .AppImage as Actions artifact
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: desktop-pet-linux
          path: release/*.AppImage

      - name: Upload .AppImage to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/*.AppImage
          fail_on_unmatched_files: false
          token: ${{ secrets.GITHUB_TOKEN }}
